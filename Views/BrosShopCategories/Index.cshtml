@model IEnumerable<WebApp2.Models.BrosShopCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BrosShopCategoryTitle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrosShopCategoryTitle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BrosShopCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BrosShopCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BrosShopCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
