@model IEnumerable<WebApp2.Models.BrosShopImage>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BrosShopImageTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrosShopProduct)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrosShopImageTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrosShopProduct.BrosShopProductId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BrosShopImagesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BrosShopImagesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BrosShopImagesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
